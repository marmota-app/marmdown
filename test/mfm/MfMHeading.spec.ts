describe('MfMHeading parser', () => {
	describe('parsing the content', () => {
		[1, 2, 3, 4, 5, 6].forEach(level => {
			it.skip(`creates a heading with leve ${level}`, () => {})
			it.skip(`creates a section with the heading\'s level "${level}", containing the heading`, () => {})
		})
		it.skip('parses simple text as the heading text', () => {})
	})
	describe.skip('parsing the content lines', () => {
		//TODO implement me
	})
	describe.skip('parsing updates', () => {
		//TODO implement me
	})
})
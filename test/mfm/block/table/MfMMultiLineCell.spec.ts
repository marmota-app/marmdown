describe('MfMMultiLineCell', () => {
	describe('parsing the content', () => {
		it.skip('parses a valid cell with contents', () => {
			expect(true).toEqual(false)
		})
	})
	describe('parsing options', () => {})
	describe('parsing updates', () => {})
})
